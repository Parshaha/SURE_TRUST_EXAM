Check win 7 vul to eternal blue.
1. Objective

The objective of this test is to assess whether a Windows 7 machine is vulnerable to the EternalBlue (MS17-010) exploit, which is a critical SMB vulnerability.
2. Required Devices & Tools
Device/Tool	Purpose
Windows 7 Machine (Target)	The system being tested for vulnerability
Kali Linux (Attacker)	Used to run penetration testing tools
CrackMapExec	SMB enumeration and vulnerability checking
Nmap	Port scanning and script-based vulnerability detection
3. Network Configuration
Device	IP Address	Role
Windows 7	192.168.35.130	Target Machine
Kali Linux	192.168.35.100	Attacker Machine
4. Methodology & Steps
Step 1: SMB Enumeration Using CrackMapExec

Command:

crackmapexec smb 192.168.35.0/24

    This command scans the 192.168.35.x subnet for active SMB shares.
    It checks whether SMB is enabled on the target machine.

Step 2: Locate the Required Nmap Script

Command:

cd /usr/share/nmap/scripts
ls | grep ms

    This verifies if the required script smb-vuln-ms17-010.nse is available.

Step 3: Scan for MS17-010 Using Nmap

Command:

nmap -p445 192.168.35.130 --script smb-vuln-ms17-010.nse

    -p445: Scans port 445 (SMB).
    192.168.35.130: Target Windows 7 machine.
    --script smb-vuln-ms17-010.nse: Runs the script to check for EternalBlue.

5. Results & Findings
Test	Result
SMB Service Detection	SMB is running on port 445
Vulnerability Check	MS17-010 Detected (System is vulnerable)

Sample Output from Nmap (if vulnerable)

Host script results:
| smb-vuln-ms17-010:  
|   VULNERABLE:
|   Remote Code Execution vulnerability in Microsoft SMBv1 servers (ms17-010)

